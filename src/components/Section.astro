---
interface Props {
  // 接收外部传入的 class
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<section class="w-full my-10 lg:my-14">
  <div class:list={["max-w-7xl px-4 mx-auto", className]} {...rest}>
    <slot />
  </div>
</section>
